# ===================================================================
# VARIABLES DE ENTORNO PARA RENDER.COM
# ===================================================================
# Copia estos valores en el Dashboard de Render:
# Dashboard → Tu servicio → Environment → Add Environment Variable
# ===================================================================

# NODE & APP CONFIGURATION
NODE_ENV=production
PORT=3000

# ===================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# ===================================================================
# Si usas Render PostgreSQL, obtén estos valores de:
# Dashboard → PostgreSQL → Connection Info
# Si usas DB externa (GCP, AWS, etc.), usa tus credenciales

DB_HOST=                    # Ejemplo: dpg-xxxxx-a.oregon-postgres.render.com
DB_PORT=5432
DB_USERNAME=                # Ejemplo: cinema_ec_user
DB_PASSWORD=                # La contraseña de tu base de datos
DB_NAME=                    # Ejemplo: cinema_ec_db
DB_SSL=true                 # Siempre 'true' en producción

# ===================================================================
# JWT CONFIGURATION
# ===================================================================
# Genera un secret fuerte: openssl rand -base64 32

JWT_SECRET=                 # Tu JWT secret (mínimo 32 caracteres)
JWT_EXPIRES_IN=7d          # Tiempo de expiración del token

# ===================================================================
# FIREBASE CONFIGURATION
# ===================================================================
# Obtén estos valores del archivo de credenciales de Firebase
# Firebase Console → Project Settings → Service Accounts → Generate Key

FIREBASE_PROJECT_ID=                    # Ejemplo: cinemaec-12345
FIREBASE_CLIENT_EMAIL=                  # Ejemplo: firebase-adminsdk-xxxxx@cinemaec.iam.gserviceaccount.com
FIREBASE_DATABASE_URL=                  # Ejemplo: https://cinemaec-12345.firebaseio.com

# IMPORTANTE: Para FIREBASE_PRIVATE_KEY en Render:
# 1. Copia la clave completa incluyendo "-----BEGIN PRIVATE KEY-----" y "-----END PRIVATE KEY-----"
# 2. Reemplaza los saltos de línea (\n) con saltos literales
# 3. O usa comillas y escapa los \n correctamente
FIREBASE_PRIVATE_KEY=                   # La clave privada completa del service account

# ===================================================================
# EMAIL CONFIGURATION (SMTP)
# ===================================================================
# Para Gmail, genera una "App Password" en:
# Google Account → Security → 2-Step Verification → App passwords

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=                              # Tu email completo (ejemplo@gmail.com)
MAIL_PASSWORD=                          # App Password de Gmail (16 caracteres sin espacios)
MAIL_FROM=CinemaEC <noreply@cinemaec.com>

# ===================================================================
# CORS CONFIGURATION
# ===================================================================
# URLs permitidas para hacer requests al backend (separadas por comas)
# Incluye todos los dominios de tu frontend

CORS_ORIGIN=https://tu-frontend.vercel.app,https://cinemaec.com,https://www.cinemaec.com

# ===================================================================
# NOTAS IMPORTANTES PARA RENDER
# ===================================================================
#
# 1. NO incluyas comillas alrededor de los valores en Render Dashboard
#    ✓ Correcto: DB_HOST = dpg-xxxxx.oregon-postgres.render.com
#    ✗ Incorrecto: DB_HOST = "dpg-xxxxx.oregon-postgres.render.com"
#
# 2. Para FIREBASE_PRIVATE_KEY con saltos de línea:
#    - Opción A: Pegarlo completo con Enter real entre las líneas
#    - Opción B: Usar \n literal: -----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n
#
# 3. CORS_ORIGIN acepta múltiples dominios separados por comas SIN ESPACIOS
#
# 4. DB_SSL debe ser exactamente 'true' (sin comillas) para SSL
#
# 5. Las variables marcadas con "sync: false" en render.yaml deben
#    configurarse manualmente en el Dashboard (son sensibles)
#
# ===================================================================
